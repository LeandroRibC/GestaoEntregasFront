services:
  - type: web
    name: gestao-frota-frontend
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npx serve -s dist
    envVars:
      - key: NODE_VERSION
        value: 22.0.0
